# Codex Task: AGENTS.md update

{{ common.send_verification_block }}

You are working in repository:

```text
{{ repo.name }}
```

## Common operating context

{{ common.timeline_operating_context }}

## Product-specific note

{{ product_note }}

## Task

Create or update `AGENTS.md` so this repository follows the current operating policy.

Requirements:

- Keep `AGENTS.md` concise but strong.
- Include repository purpose.
- Include non-destructive work rules.
- Include approval-required actions.
- Include fixed input/output directory policy if relevant.
- Include Job/Run/InputSource concepts if relevant.
- Include Docker/local-first/security rules if relevant.
- Include reporting format.

After updating, report changed files and any unresolved ambiguity.
Echo the same Run ID in the report.
